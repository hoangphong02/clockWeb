import React from "react";
import ReactToPrintComponent from "../../components/ReactToPrintComponent/ReactToPrintComponent";

const TestPage = () => {
  return <ReactToPrintComponent />;
};

export default TestPage;
